# 진행 백로그 — Claude에게 시킬 명령 스크립트 (초안)

추후 세션에서 **그대로 복사·붙여넣어** 작업을 지시하기 위한 프롬프트 모음입니다.
각 항목: `명령` + (참고 파일) + ✅수용 기준.

> 작업 전 항상 지켜야 할 규칙(요약): 자격증 표현 금지 · Spharos 디자인(`spharos_design.md`, 색 띠 금지·웨지 없음) · UI=Pretendard/코드=D2Coding · 신규 코드는 `assets/`로 분리 · 커리큘럼 원천은 `curriculum/` · 브라우저 도구로 테스트 금지(로컬 서버 + 사용자 확인) · 배포 시 `?v=` 캐시 버전 올리기.

---

## 0. 세션 시작 (컨텍스트 로드)
```
이 저장소의 INSTRUCTIONS.md, STRUCTURE.md, curriculum/ROADMAP.md, spharos_design.md 를 먼저 읽고 현재 구조를 파악한 뒤 작업 시작해.
```
✅ 아카데미 2-트랙 구조·Spharos 규칙·파일분리 원칙을 반영한 상태로 응답.

---

## 1. 신규 과정 콘텐츠 제작 (최우선)
기획 완료된 **고성능 DB 설계**(트랙 B·L3)부터 구현. (참고: `curriculum/design/02-highperf-db-design.md`)

### 1-1. 과정 셸/메뉴 먼저
```
'고성능 DB 설계' 과정을 신규로 추가해. 홈 로드맵의 해당 노드를 '운영 중'으로 바꾸고, 입장 시 이 과정 전용 사이드바(세션1~6)가 뜨도록 과정 단위로 분리 구현해. 코드는 assets/js/courses/design/ 와 패널 partial 로 분리.
```
✅ 홈에서 입장 → design 과정 전용 메뉴 표시 / 성능진단 소모임과 독립.

### 1-2. 신규 핵심 랩 (이 과정의 차별점, 하나씩)
```
'파티션 라이프사이클 매니저' 랩을 만들어줘. curriculum/design/02 세션5 기준: 월별 Interval 파티션 자동생성 → 보존기간 만료 파티션 DROP/TRUNCATE → Exchange 적재 → 프루닝 확인. 인터랙티브(시뮬레이터)로, Spharos 테마·D2Coding·ij-plan-table 재사용.
```
```
'정규화 변환기' 랩을 만들어줘. 비정규 테이블 → 1·2·3NF 단계 분해, 삽입/수정/삭제 이상 데모, 반정규화 시 조회↔갱신 비용 비교. (세션1)
```
```
'테이블스페이스 공간 관리 대시보드' 랩을 만들어줘. 세그먼트 증가 추이·사용률, HWM·단편화 진단, 공간부족 대응. (세션4, 기존 대시보드 패턴 재사용)
```
✅ 각 랩: 별도 파일 분리 · 시나리오/채점 또는 시뮬레이터 패턴 · 테마 준수 · 로컬서버로 확인 안내.

### 1-3. 나머지 과정 (순차)
```
curriculum/foundation/sql-foundation.md 기준으로 'SQL 기초' 과정을 만들어줘. (선수과정, 챌린지/퀴즈 재사용)
```
```
curriculum/design/01-data-modeling.md 기준으로 '데이터 모델링' 과정을 만들어줘.
```

---

## 2. 멀티 과정 라우팅 강화
```
과정별로 사이드바 메뉴가 전환되도록 라우팅을 일반화해줘. 과정 진입 시 해당 과정의 메뉴/패널만 보이고, 다른 과정 진입 시 교체. academy.js 의 과정 정의(데이터)로 구동되게.
```
✅ 2개 이상 과정이 각자 메뉴를 갖고 충돌 없이 전환 · 상단 인디케이터 과정명 동기화.

---

## 3. 파일 분리 마이그레이션 (STRUCTURE.md 2~5단계, 단계별·검증 동반)
```
app.js 의 랩 함수군을 주제별 파일(assets/js/labs-*.js)로 분리하고 순서대로 로드하도록 바꿔줘. 동작 깨지지 않게 한 번에 하나씩, 매번 문법검사+로컬서버 확인.
```
```
index.html 의 과정별 패널 HTML을 partial 파일로 분리하고, 과정 진입 시 fetch로 지연 로딩하도록 바꿔줘. (페이지 로딩 최적화)
```
```
학습자료의 인라인 마크다운(<script type="text/markdown">)을 session_*.md fetch 로 외부화해줘.
```
✅ index.html/app.js 용량 감소 · 기존 기능 회귀 없음.

---

## 4. 디자인 마무리 (Spharos)
```
라이트 테마에서 남은 다크 컨테이너(rgba(0,0,0,0.x) 배경)를 전수 점검해서, 텍스트 영역은 밝게 정리해줘. 진행률 막대 트랙 등 의도적 다크는 보존.
```
```
사이트에 옐로우 포인트를 spharos_design.md 원칙대로 소량 추가해줘(활성 강조 등). 단 색 띠(stripe)는 금지, 옐로우 위 텍스트는 잉크/네이비.
```
```
spharos_design.md §5·§7.10 대로 좌하단 로고 + 카피라이트 푸터를 추가해줘. (로고 자산 경로 확인 필요)
```
✅ 대비 4.5:1 · 띠/웨지 없음 · 60·30·10 비율 유지.

---

## 5. 운영 / 기타
```
접속자 집계 visit_stats.sh 로 최근 N일 리포트를 보여줘. (서버에서 sudo 실행 안내)
```
```
배포 체크리스트대로 점검해줘: sudo -u nginx git pull, ?v= 캐시 버전, Nginx __visit location, .md 차단.
```

---

## 작업 묶음 추천 순서
1. (1-1) 고성능 DB 설계 과정 셸 → (1-2) 핵심 랩 3종
2. (2) 멀티 과정 라우팅 일반화
3. (1-3) SQL 기초 → 데이터 모델링
4. (3) 파일 분리 마이그레이션
5. (4) 디자인 마무리

> 한 번에 한 묶음씩, 끝나면 로컬서버 확인 → 커밋·푸시.
